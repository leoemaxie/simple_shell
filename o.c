#include "hash.h"

int main(int ac, char **av, char **env)
{
	tokenize("hellp world");
	//if (tokens == NULL)
	//	return (-1);
	//printf("%s, %s", tokens[0], tokens[1]);
	//free_tokens(tokens);
	//setenv("HOME", "NULL", 1);
	//puts(env[31]);
	//printenv();
	//printenv();
	return (0);
}
